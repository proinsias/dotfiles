## Supported server options to pass to vncserver upon invocation can be listed
## in this file. See the following manpages for more: vncserver(1) Xvnc(1).
## Several common ones are shown below. Uncomment and modify to your liking.
##
# securitytypes=vncauth,tlsvnc
# desktop=sandbox
# alwaysshared
# interface=127.0.0.1  # explicit ipv4 localhost address
# httpd=""  # disable http server (port 5800)

#geometry=1920x1080
geometry=2048x1152
# geometry=3440x1440
# Only allow connections from the same machine. Useful if you use SSH and want to stop non-SSH connections from any other hosts.
localhost
