addpath = '/Users/{{ .chezmoi.username }}/idl_lib/:'
!PATH = expand_path('+' + !DIR + ':' + 'external' + ':' + !PATH)

if !path eq '' then $
  !path = addpath $
else !path = addpath + ':' + !path
;delvar, addpath

;astrolib

;@/Users/{{ .chezmoi.username }}/idl_lib/PhysicalConstants.pro
