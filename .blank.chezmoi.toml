encryption = "age"

[data]
    name = "Name"
    email = "name@example.com"
    defaultkey = "123"
    githubemail = "name@example.com"
    opaccount = "opaccount"
    slackrecipient = "slackrecipient"
    slacktoken = "123"
    work = false
    # Personal machine configuration

[age]
    # passphrase = true
    identity = "~/.config/chezmoi/key.txt"
    recipient = "123"

[diff]
    command = "code"
    args = ["--wait", "--diff"]

[edit]
    command = "code"
    args = ["--wait"]

[git]
    autoCommit = false
    autoPush = false

[gpg]
    args = ["--quiet"]
    symmetric = true

[[textconv]]
    pattern = "**/*.plist"
    command = "plutil"
    args = ["-convert", "xml1", "-o", "-", "-"]
