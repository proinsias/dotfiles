{{  if ne .chezmoi.os "windows" }}
#!/usr/bin/env bash

set -euo pipefail

echo
echo
echo "Adding symbolic links for oh-my-zsh aliases."

mkdir -p ~/.oh-my-zsh/custom
ln -f -s ~/.config/zsh/aliases.zsh ~/.oh-my-zsh/custom/aliases.zsh
{{ end }}
